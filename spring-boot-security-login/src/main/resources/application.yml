server:
  port: 8080
  servlet:
    context-path: /login


spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123
    url: jdbc:mysql://localhost:3306/study_db?useUnicode=true&characterEncoding=utf8&serverTimezone=Hongkong&useSSL=false
  application:
    name: login-in-system

eureka:
  client:
    service-url:
      defaultZone:  http://localhost:7001/server/eureka
  instance:
    instance-id: login-system-8080
    prefer-ip-address: true



mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.lsy.demo.repo.model
  global-config:
    db-config:
      logic-not-delete-value: 1
      logic-delete-value: 0
  config-location: classpath:mybatis-config.xml
